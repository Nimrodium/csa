package game;
/*
enum Guess:
	Character (char c);
	Phrase (String s); 
	+ enter() -> Guess;
	- new(String s) -> Guess;
	- new(char c) -> Guess;
*/
public class Guess {
    public enum GuessVariants{
        CHARACTER, PHRASE;   
    }
    
}
